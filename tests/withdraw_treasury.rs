use anchor_lang::prelude::*;
use based_protocol::*;

#[test]
fn ensure_withdraw_treasury_included() {
    // Dummy test to mark the function as "used"
    // This forces Anchor to include it in the IDL
    let _dummy = "withdraw_treasury exists";
    assert!(true);
}
